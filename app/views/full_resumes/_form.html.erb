<%= form_for(@full_resume) do |f| %>
  <% if @full_resume.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@full_resume.errors.count, "error") %> prohibited this full_resume from being saved:</h2>

      <ul>
      <% @full_resume.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
