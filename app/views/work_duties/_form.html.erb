<%= form_for(@work_duty) do |f| %>
  <% if @work_duty.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@work_duty.errors.count, "error") %> prohibited this work_duty from being saved:</h2>

      <ul>
      <% @work_duty.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :work_id %><br>
    <%= f.text_field :work_id %>
  </div>
  <div class="field">
    <%= f.label :task %><br>
    <%= f.text_field :task %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
